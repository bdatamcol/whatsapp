// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Configuración absolutamente mínima
  reactStrictMode: true,
  // Eliminamos toda configuración de webpack
};

export default nextConfig;